```@meta
CurrentModule = DoubleGyre
CollapsedDocStrings = true
```

# Animation

The results of [`advect_particles`](@ref) can be animated with the `animate_particles` function.

```@docs
animate_particles
```